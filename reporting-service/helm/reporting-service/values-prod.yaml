replicaCount: 3

autoscaling:
  enabled: true
  minReplicas: 3
  maxReplicas: 10
  targetCPUUtilizationPercentage: 70
  targetMemoryUtilizationPercentage: 80

resources:
  requests:
    cpu: "500m"
    memory: "512Mi"
  limits:
    cpu: "2000m"
    memory: "1Gi"

config:
  kafkaBootstrapServers: "${MSK_BOOTSTRAP_SERVERS}"
  esUris: "${OPENSEARCH_URIS}"
  keycloakJwksUri: "${KEYCLOAK_JWKS_URI}"
  redisHost: "${ELASTICACHE_HOST}"
  postgresUrl: "${RDS_URL}"
  logLevel: "INFO"

podDisruptionBudget:
  enabled: true
  minAvailable: 2

ingress:
  hosts:
    - host: reporting.banking.com
      paths:
        - path: /api/reports
          pathType: Prefix
  tls:
    - secretName: reporting-tls
      hosts:
        - reporting.banking.com

affinity:
  podAntiAffinity:
    requiredDuringSchedulingIgnoredDuringExecution:
      - labelSelector:
          matchExpressions:
            - key: app
              operator: In
              values:
                - reporting-service
        topologyKey: kubernetes.io/hostname
