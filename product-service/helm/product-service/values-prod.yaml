replicaCount: 3

autoscaling:
  enabled: true
  minReplicas: 3
  maxReplicas: 10
  targetCPUUtilizationPercentage: 70
  targetMemoryUtilizationPercentage: 80

resources:
  requests:
    cpu: "500m"
    memory: "512Mi"
  limits:
    cpu: "2000m"
    memory: "1Gi"

config:
  kafkaBootstrapServers: "${MSK_BOOTSTRAP_SERVERS}"
  postgresUrl: "${RDS_URL}"
  logLevel: "INFO"

podDisruptionBudget:
  enabled: true
  minAvailable: 2

ingress:
  hosts:
    - host: products.banking.com
      paths:
        - path: /api/products
          pathType: Prefix
  tls:
    - secretName: product-tls
      hosts:
        - products.banking.com

affinity:
  podAntiAffinity:
    requiredDuringSchedulingIgnoredDuringExecution:
      - labelSelector:
          matchExpressions:
            - key: app
              operator: In
              values:
                - product-service
        topologyKey: kubernetes.io/hostname
